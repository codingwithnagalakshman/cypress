{
  "dependencies": {
    "cypress": "^12.17.3"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:override": "cypress run --browser edge --config viewportWidth=1280,viewportHeight=720",
    "cypress:parallel": "cypress run --record --key=abc123 --parallel"
  }
}
